{*
 * SSL mapping admin skeleton.
 * Backend actions are handled by AdminNtRcSslController / module configuration.
 * No provider API call is executed from this template.
 *}
<div class="panel">
  <h3>{l s='SSL Product Mapping' mod='ntresellerclub'}</h3>
  <p>{l s='SSL mapping backend is ready. Full UI design will be completed in a later engine.' mod='ntresellerclub'}</p>
</div>
